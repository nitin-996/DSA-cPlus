// Bitwise operator

// and = & 
// or = |
// not = ~
// xor = ^ (caret)

// IN And bitwise operator it only return 1 when both inputs are same 1 1.
// in OR bitwise operator it only returns 0 when both inputs are same 0 0.
// IN xOR bitwise operator it returns 1 when both inputs are diffrent 0 1. it returns 0 when both inputs are same 1 1.
// IN not bitwise operator if input is 1 then it convert it into 0 and if input is 0 then it convert it into 1. 

// >> it is right shift operator. which moves bits to right side.and at the end add 0.in right shift binary divided by 2.
// << it is left shift operator. which moves bits to right side and at the start add 0.in left shift binary multiplied by 2.

// arithmatic operators ki precedence control kerne ke lia () parathesis braket ka use kero.
